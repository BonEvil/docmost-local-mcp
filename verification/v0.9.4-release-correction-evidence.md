# v0.9.4 release-correction evidence

Status: **passed** for the source correction prepared from BonEvil/main commit
`39ca53c187a14f58235be68c7b2a19b16e768ac2`. The implementation commit before
adding retained evidence was `2da23da5f5cb722e72f01be99f08d8833d76d4fc`
with tree `945b962c300b2c5efd7ea5cfd69fedba15a17e87`.

## Corrected Sigstore contract

The release workflow pins official `sigstore/cosign-installer` v4.1.2 to full
commit `6f9f17788090df1f26f669e9d70d6ae9567deba6` and pins cosign v3.0.3. The exact
action source selects and verifies both `cosign-linux-amd64.sigstore.json` and
`cosign-linux-amd64-kms.sigstore.json` for cosign v3. The deterministic fixture
records those published assets and the regression proves the absent legacy
`cosign-linux-amd64.sig` request fails.

`tests/release_integrity_test.sh` separately proves that missing, malformed,
mismatched, unapproved-redirect, oversized, or unverified release-manifest
verification bundles fail closed, preserve an existing executable, and leave no
staging files. Transport host allowlisting, redirect limits, byte caps, manifest
identity/digest checks, cosign workflow identity and issuer verification, and
atomic replacement remain enforced by `scripts/install-atlas.sh`.

## Retained local results

The complete sanitized command outputs are under
`verification-logs/v0.9.4-release-correction/`. `cargo fmt --check`, locked Cargo
metadata, warnings-denied all-target/all-feature Clippy, both locked test modes,
and the locked no-default release build passed in the digest-pinned Rust 1.98.0
container with a read-only source mount. Each locked test mode reports 138 passed
and zero failed. Release-integrity, cosign-contract, version-consistency,
release-context, immutable-input, launcher, syntax, integrated-evidence, and
`git diff --check` results are retained in `security-release-gate.log`.

Cargo-deny 0.20.2—the version selected by the repository's full-SHA-pinned
action—reported advisories, bans, licenses, and sources all OK. Its concise
result is retained in `cargo-deny.log`.

`SHA256SUMS` binds every retained log relative to its own directory.

## Tag and no-publication boundary

The sanitized provider read-back in
`verification/v0.9.4-release-correction-provider-evidence.json` establishes that
the annotated v0.9.3 tag object remains
`1f431aa821c628791b6151cd561909470ecd4f65`, targets the unchanged main commit
`39ca53c187a14f58235be68c7b2a19b16e768ac2`, and has provider verification
`verified=true`, `reason=valid`. The v0.9.3 and v0.9.4 release endpoints both
returned 404, v0.9.4 had no tag ref, and the Atlas card branch had no remote ref.

The companion `verification/v0.9.4-no-publication-ledger.json` records the
card's empty external-mutation set and each explicitly prohibited action. No
credential, signature bytes, signing material, token, cookie, private Docmost
content, or private service identifier is retained.
