# v0.9.3 sealed-candidate provenance and gate inventory

Status: **passed**. This record binds the coherent v0.9.3 source candidate,
both refreshed evidence tracks, and the complete local/hosted-equivalent gate
run. It contains no Docmost origin, credentials, session material, private
content, or live-service response data.

## Candidate lineage

| Role | Commit | Tree |
| --- | --- | --- |
| Prepared v0.9.3 source and initial checks | `a33895bef1c66c3bb0855c4d2ee06cef252c020f` | `327d122cf9695b586e6999142a101b86bda4f67a` |
| Atlas-control evidence | `7bbcaf2e245c3496b126ed0c358cb7885227491a` | `cf93c966e9fccd023c28fd814b82f56d36373814` |
| Docmost v0.95.0 evidence | `8a748a262064f2b733c904b5fcf12d47cc932dd5` | `cbe6067731148df025ce069bbeb10e03ec99d4d7` |
| Mechanically reconciled test baseline | `fb7288daf47ad2cf6a6cd3f465377508efca0141` | `cbe6067731148df025ce069bbeb10e03ec99d4d7` |

The prepared candidate and integration commit `b93024c6094abe5044b56f32991e39d650c6c9e5`
share source tree `327d122cf9695b586e6999142a101b86bda4f67a`.
Every change from that prepared source candidate through the tested baseline is
under `verification/`; no source, lockfile, manifest, workflow, launcher,
release script, configuration, or policy input changed. The final seal adds
only this provenance record and checksum-bound transcripts under
`verification-logs/v0.9.3-seal/`.

The definitive candidate identity is the seal commit carrying this record.
Its commit message records the staged content-tree identity, while the Atlas
handoff records both the resulting commit and tree. This split avoids the
impossible self-reference of storing a Git commit's own hash inside its tree.

## Version consistency

`Cargo.toml`, the root package entry in `Cargo.lock`,
`npm/launcher/package.json`, and the README release references all identify
version `0.9.3`. The retained `version-consistency.log` contains the inspected
lines.

## Exact gate inventory

All source checks used Rust `1.98.0` and the committed lockfile. The Linux ARM64
test environment used the repository-pinned
`rust:1.98.0-slim-bookworm@sha256:1469a27c125cb5a3aebfa4f4e4665d935b02fb72cc093b2c974b3d740e43f157`
image with the repository mounted read-only and a temporary target directory.

| Gate | Exact command or equivalent | Result |
| --- | --- | --- |
| Formatting | `cargo fmt --check` | passed |
| Warnings-denied lint | `cargo clippy --locked --all-targets --all-features -- -D warnings` | passed |
| All-feature tests | `cargo test --locked --all-features` | passed, 138 tests and 0 failures |
| No-default-feature tests | `cargo test --locked --no-default-features` | passed, 138 tests and 0 failures |
| Lockfile resolution | `cargo metadata --locked --format-version 1 --no-deps` | passed |
| Advisory/supply-chain policy | `cargo deny --offline check advisories bans licenses sources` with cargo-deny 0.20.2 and RustSec database `ba9db2a77a6a0fe93bc63a3d9b730e08b145aff5` | advisories, bans, licenses, and sources passed; configured duplicate notices remained warnings |
| Launcher smoke | CI-equivalent mock binary invocation plus `node --check npm/launcher/postinstall.js` | passed |
| Release integrity | `bash tests/release_integrity_test.sh` | success and every rejection path passed |
| Release context | `bash tests/release_context_test.sh` | success and every rejection path passed |
| Immutable inputs | `bash scripts/check-pinned-inputs.sh` | passed |
| Integrated security/release assertions | terminal job's evidence, Docmost version, disabled-deployment, and `git diff --check` assertions | passed |

No provider check is claimed: these Atlas-local commits were not published and
PR/merge work is outside this card. Instead, every CI job command was executed
locally and all six configured release targets produced a complete optimized
binary. Cross-build tooling and caches were isolated to the card workspace.

| CI platform | Build | Output inspection | SHA-256 |
| --- | --- | --- | --- |
| `darwin-arm64` | native `cargo build --locked --release --no-default-features --target aarch64-apple-darwin` | Mach-O 64-bit arm64 | `482f831d349156a8e8f7aba270ca7dce784666dec02df40bc416d0c5b31cfa85` |
| `darwin-x64` | native-SDK cross-build for `x86_64-apple-darwin` | Mach-O 64-bit x86_64 | `289e2d105f1610c9ba64f36b1fd261ff18e0bb003f5642c45e8db4c49d75d88e` |
| `linux-arm64` | pinned ARM64 Rust container | ELF 64-bit ARM64 | `ebcfffc21896f6ed73f1747f983cc5deff2fbad7d7069dd0cbff25e4ce55c795` |
| `linux-x64` | cargo-zigbuild 0.23.3 with checksum-verified Zig 0.16.0 | stripped ELF 64-bit x86_64 | `feb0e97b52b1aca8c005d8f71ee58a30d71aaa9600e91b574e6073c7b98acca3` |
| `win32-arm64` | cargo-xwin 0.23.1 with isolated MSVC CRT/SDK | PE32+ console ARM64 | `27ad311ae063342152e96a55ac909c2ff720c166e4fa20d6b112e32b8b06a7bb` |
| `win32-x64` | cargo-xwin 0.23.1 with isolated MSVC CRT/SDK | PE32+ console x86-64 | `e29c46da9cb7a9850bb1cab4d91617d7bd57bd68e1650785f692ad106a5b863e` |

The Linux x64 linker emitted one non-fatal warning that Zig ignored the
deprecated optimization spelling `1`; the optimized build completed and the
binary format and digest were inspected. No project warning or source change
resulted.

## Refreshed evidence applicability

- `verification/docmost-v0.95.0-compatibility-report.md` and
  `verification/docmost-v0.95.0-live-evidence.md` bind the Docmost v0.95.0
  matrix and corrective hostile-loopback tests to source tree
  `327d122cf9695b586e6999142a101b86bda4f67a`, with cleanup complete.
- `verification/v0.9.3-atlas-control-evidence.md` and its JSON companion bind
  Atlas authorization, replay, expiry, hostile-network, redaction, and cleanup
  results to that same source tree.
- The seal changes after the prepared candidate are verification-only.
  Therefore neither live evidence track is invalidated, while the full local
  suite above independently rechecks the integrated repository state.

## Retained evidence

`verification-logs/v0.9.3-seal/SHA256SUMS` binds every retained successful
transcript. Validate from that directory with
`shasum -a 256 -c SHA256SUMS`. Failed environment-probing attempts were not
retained or represented as candidate failures.
