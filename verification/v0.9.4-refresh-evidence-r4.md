# Exact-candidate v0.9.4 refreshed evidence (R4)

Status: **passed** for source candidate
`eed531af611739141986dbb62907db8416bce5d6`, tree
`a2172f86dfa44b717d6291eea97554c54dc7ef65`, inherited through integration
commit `b7a44ea6f9150bf8abd491af3e781ba79c0c08ee`. The integration descendant
adds only verification records after the source candidate. This card did not
publish, merge, tag, release, install, activate, or access an ordinary Docmost
endpoint.

## Provenance and candidate gates

The exact candidate was exported without Git metadata into a read-only mount
and built twice in separate targets with Rust 1.98.0 from the digest-pinned
Linux x86-64 image. Both release binaries have SHA-256
`c2780277b10d5ee360f58f1c51b1386517ae178c35a2350f239008ee111d17d0`.
The tested binary was rehashed before teardown. `Cargo.lock` SHA-256 is
`1f985207a91681d799285d4ae580fbbbd9be8a56a6ba6070de85b5331f174a7d`.

The locked all-feature and no-default-feature suites each passed 162 tests
(with the opt-in live test correctly ignored during ordinary runs), alongside
warnings-denied all-target Clippy and formatting. The opt-in live delete test
then passed separately against disposable v0.95.0 infrastructure. Release
integrity, cosign-installer, version, signed-context, immutable-input,
`git diff --check`, and cargo-deny advisories/bans/licenses/sources gates all
passed. The release workflow's exact six-target matrix remains statically
locked; this no-publication card executed the available Linux x86-64 build and
inherits the Linux ARM64 compiled-input result from the exact dependency
candidate. Hosted Darwin and Windows release jobs were not invoked because
that would require publication-side provider work outside this card.

## Disposable Docmost Community v0.95.0 matrix

The stack used digest-pinned Docmost v0.95.0, Postgres 16, and Redis 7.2 images
in a uniquely labelled three-container/three-volume project on an unused
loopback listener. Identity and content were synthetic; secrets were generated
inside a mode-0700 card directory and never retained.

A default process exposed exactly the ten accepted reads and zero writes. All
thirteen accepted write names failed as unavailable. A separate five-write
legacy allowlist exposed only its exact subset; the other eight remained
unavailable. All ten reads and six legacy mutations passed, followed by fresh
readback. Forced expiry rejected silent reuse, session-only reauthentication
restored access, state modes were 0700/0600, no credential file existed, and
origin-scoped forget removed all candidate state.

The three delete tools then passed independent live effects: two exact pages
were observed trashed, two exact comments were observed absent, and an exact
space/page/comment chain was observed absent. All delete results were
structured, target-bound, consequence-bearing, and declared no automatic
retry. Modal and delete fixtures were independently cleaned before stack
teardown.

Dedicated hostile origins proved redirect refusal with zero target hits, the
30-second request deadline, declared and streamed 8 MiB response bounds, a
4097-byte pre-dispatch input rejection, sanitized 403 and 500 failures, process
reuse, and zero credential/content/origin/URL/canary leakage.

## Atlas inventory, authorization, and destructive modal

Current Atlas commit `83ccb1e453f0db80d3bfa98309a611f08e38d361`
added the default candidate, negotiated `2025-03-26`, returned exactly ten
read-only tools and zero writes, persisted the record, deleted it with 204, and
read back zero registrations.

Reviewed authorization runtime
`efad3719b67fc9949be3809a7d07b297a64de10d` exercised the exact candidate's
thirteen-write inventory against disposable content. For every delete tool in
both persisted Normal and YOLO modes, Atlas classified the effect as
destructive and presented the sanitized modal before dispatch. Deny, dismiss,
expiry, wrong-session mismatch, target-digest mismatch, and turn interruption
each produced zero dispatch and independently preserved the exact target.
Concurrent approve-once dispatched exactly one call, consumed the competitor,
returned 409 on replay, and produced one independently observed Docmost effect.
The complete matrix therefore records six approved disposable dispatches,
zero unauthorized dispatches, six exact effects, and zero remaining
registrations.

## Cleanup and retained evidence

Direct teardown removed every card container, volume, network, synthetic
database/content item, identity, session, credential, source export, build
target, binary, Cargo home/cache, cargo-deny binary, Atlas database/export,
registration, and harness. The stable ordinary inventory, container start-time,
and Serve hashes are byte-identical before and after; the ordinary listener is
present and the disposable listener absent.

The checksum manifest binds this report and all R4 direct logs. No retained
file contains a credential, private response value, synthetic target UUID,
private origin, or ordinary private content. This evidence authorizes no
publication, release, installation, activation, or production registration.
