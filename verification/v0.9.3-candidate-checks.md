# v0.9.3 candidate check record

This record retains the required local candidate-gate results for the initial
v0.9.3 preparation commit
`dcf68a8272b61da2e032aa46ec6550f9f60439c2` (tree
`20012cbb47a2a15a8fc366aeda52944589e77aaa`). It contains no Docmost origin,
credentials, private content, or live-service output.

The host did not provide `cargo` or `rustfmt`, so the commands ran in the
repository-pinned `rust:1.98.0-slim-bookworm` image (digest
`sha256:1469a27c125cb5a3aebfa4f4e4665d935b02fb72cc093b2c974b3d740e43f157`)
with the candidate mounted read-only and `CARGO_TARGET_DIR` set to a container
temporary directory. The sequential command exited with status 0:

```text
cargo fmt --check
cargo test --locked --all-features
cargo test --locked --no-default-features
```

The retained terminal output reports successful test runs with no failures,
including the final no-default-feature run. No repository files were written by
the containerized checks.
