# Exact-candidate v0.9.4 refreshed evidence (R2)

Status: **passed** for inherited candidate
`b16298f47148f7575725e27bdcec943e0a13c272`, tree
`37744d4f533fc1c55b37e86497a6b799fe4b4f2a`, integrated unchanged as
`28ea2c571be70f7023e396d487c99dd77d02fd49`. The integration and inherited
commits resolve to the same tree, and their non-verification source diff is
empty. This card did not publish, merge, tag, release, install, activate, or
touch an ordinary Docmost endpoint.

The machine-readable summary is
[`v0.9.4-refresh-evidence-r2.json`](v0.9.4-refresh-evidence-r2.json). Raw,
sanitized observations are retained in `verification-logs/` and are bound by
that directory's `SHA256SUMS`.

## Provenance and reproducibility

The lockfile SHA-256 is
`1f985207a91681d799285d4ae580fbbbd9be8a56a6ba6070de85b5331f174a7d`.
The exact tree was exported without Git metadata and mounted read-only into
`rust:1.98.0-slim-bookworm@sha256:1469a27c125cb5a3aebfa4f4e4665d935b02fb72cc093b2c974b3d740e43f157`.
Two clean Linux x86-64 release builds, each with its own target directory,
produced the identical binary SHA-256
`a28539da4bd086f9a0d6b137a6525b77171b87fa986ac5f26b6d0f792e5d15a0`.
The binary used for Docmost and Atlas observations was rehashed before cleanup.

The exact tree also passed the full locked suite natively on Linux ARM64: 152
tests in all-features mode and 152 in no-default-features mode, warnings-denied
all-target/all-feature Clippy, and formatting. The dependency card separately
bound the same tree to a clean macOS ARM64 release binary and current-Atlas
add-flow. This card independently repeated current-Atlas inventory using the
Linux x86-64 binary over the approved private execution boundary.

Release-integrity, cosign-v3 installer contract, version consistency, release
context, immutable-input, `git diff --check`, and cargo-deny advisories, bans,
licenses, and sources gates all passed. The first slim-container shell-gate
invocation lacked `jq`; no repository gate was weakened, and the exact shell
gates were rerun immediately with the host's `jq` and passed.

## Disposable Docmost Community v0.95.0 matrix

The digest-pinned Docmost image ran with Postgres and Redis in a uniquely named
three-container, three-volume Compose project bound only to the unused loopback
port `3301` behind the already-existing private `:8443` route. All content,
identity, space, page, and comment values were synthetic. Secrets were generated
inside a mode-`0700` disposable directory, used session-only, and never retained.

The server package reported `0.95.0`; MCP negotiated `2025-03-26`. A fresh
default process exposed exactly the accepted ten tools, all read-only, and all
ten mutation names returned `tool not found`. A separate process exposed only
five allowlisted writes (`create_page`, `update_page`, `move_page`,
`create_comment`, `update_comment`); the other five mutation names remained
unavailable. The process made two synthetic page creations and one update,
move, comment creation, and comment update. A new default process completed the
remaining reads and observed the intended result.

Direct read-only inspection of only the disposable database returned:

```text
synthetic_pages=2
nested_child=1
updated_page_body=1
synthetic_comments=1
updated_comment_body=1
```

A forced-expired session was not silently reused, a fresh session-only login
restored a read, state modes were `0700`/`0600`, no credential file existed,
and origin-scoped forget removed all state.

## Hostile paths and sanitation

Dedicated loopback origins returned redirect, stalled, declared-oversize,
chunked-oversize, `403`, and `500` responses. The redirect target received zero
requests; timeout occurred at 30.0 seconds; both response forms stopped at the
8,388,608-byte limit; a 4,097-byte search was rejected before network dispatch;
and the 718-byte hostile `403`/`500` bodies were omitted. The MCP inventory was
re-enumerated after each failure. Candidate-visible diagnostics contained zero
canary, origin, URL, response excerpt, credential, or private-content values.

## Atlas authority and dispatch

Current Atlas commit `83ccb1e453f0db80d3bfa98309a611f08e38d361`
added the exact candidate through its actual persisted server API, negotiated
`2025-03-26`, returned exactly ten read-only tools, persisted and read the
record back, deleted it with status 204, and retained zero registrations.

The reviewed authorization runtime commit
`efad3719b67fc9949be3809a7d07b297a64de10d` (tree
`58d8b8c5d330c905ef70b5be33b06883c0a57ae6`) then ran the exact candidate
against fresh hostile synthetic origins. A read-only MCP annotation did not
grant authority: Atlas independently classified the call as effect `write` and
held two concurrent exact calls. Approve-once dispatched exactly one; the
competing call was already consumed and replay returned HTTP 409. Separate deny
and expiry phases had null dispatch outcomes. A clean server-side hit ledger
recorded exactly one approved permission-denied request and zero requests for
the denied and expired origins. Each phase deleted its registration and read
back an empty inventory. No test write or production access occurred.

## Cleanup and ordinary-service invariance

Cleanup removed all three disposable containers, three volumes, network,
synthetic database/content, generated identity, sessions, credentials, source
exports, both build targets, Cargo homes/caches, cargo-deny binary, harnesses,
temporary Atlas databases/registrations, tested binaries, and the full Rust
image pulled by this card. Post-cleanup label and path checks returned zero.

The ordinary container inventory, container IDs/start times, and complete
Tailscale Serve configuration retained their exact pre-test SHA-256 values:

```text
ordinary_inventory_sha256=17c351c06381f653bd2332f1db47eaeefdf7c480819510ff394c924689408c80
ordinary_started_sha256=7702be144f44fbcc5e040570cf2906f619e97e281382c9001b31e5434be6e007
serve_config_sha256=45a54a9f3b304d934f54bb66853ebcebae88337677d65b6dfadc073c7aa11401
ordinary_3001_listener=present
disposable_3301_listener=absent
```

This evidence establishes exact-candidate compatibility and control behavior.
It authorizes none of publication, release, installation, activation, or
production registration.
