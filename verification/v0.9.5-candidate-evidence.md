# Exact-candidate v0.9.5 verification evidence

Status: **passed** for source commit
`98b328da6819735e1da44fa736cb37383c389b5d`, tree
`c57b88f9f56adfa7836cd7df32cf3488b5b6c745`. This source is the inherited
v0.9.5 product-identity correction plus the bounded evidence workflow. Every
later candidate change is confined to `verification/` or `verification-logs/`.

No merge, tag, release, installation, activation, production registration, or
ordinary Docmost request occurred.

## Native platform matrix

Provider run `33620406860`, attempt `1`, completed all six native jobs
successfully. Each runner checked out the exact source commit and tree, built
from `Cargo.lock` with Rust 1.98.0, executed its native release binary, required
the exact output `docmost-local-mcp 0.9.5`, calculated SHA-256, and inspected
the executable header.

| Platform | Target | Format | SHA-256 | Provider job |
| --- | --- | --- | --- | --- |
| Darwin ARM64 | `aarch64-apple-darwin` | Mach-O ARM64 | `605f257e04198bd22ee2bdf09e330d0a1c02af54ffde9c53d83fd78e80d98070` | `100215881630` |
| Darwin x64 | `x86_64-apple-darwin` | Mach-O x86-64 | `3900c675a3914d0042bee039777f1cb4220bd1bceb8e388edcf6c7ebb9047169` | `100215881657` |
| Linux ARM64 | `aarch64-unknown-linux-gnu` | ELF ARM64 | `615b72482449a62d1502fef531fcdab63354a50980f2e9223d7a48c7069f4e70` | `100215881667` |
| Linux x64 | `x86_64-unknown-linux-gnu` | ELF x86-64 | `6609cef388967a9c74aba84651189eead1edc96cec625c52f78d32d339b3368f` | `100215881567` |
| Windows ARM64 | `aarch64-pc-windows-msvc` | PE/COFF ARM64 | `b669135eb499933fd511995a5a347b3373f6f2b89bd93a432bf883c61d59d22c` | `100215881302` |
| Windows x64 | `x86_64-pc-windows-msvc` | PE/COFF x86-64 | `8be8595e87830fd2db1bc97716912b214b80ba8258373d72e56013799f5b0344` | `100215881625` |

The complete direct transcripts, provider job/artifact records, artifact sizes,
and checksum manifest are under `verification/v0.9.5-platform/`. The binaries
were retained by the provider for one day only and were removed from the card
workspace after independent hash validation; binaries are not committed.

The automatic retention job created the correct evidence commit but its push
lost a non-fast-forward race to the repository's older v0.9.4 retention
workflow. The immutable artifacts from the successful six-job run were then
downloaded, rehashed against their direct transcripts, format-checked, scanned,
and retained locally. No build was reused or inferred.

## Repository and product gates

The exact source was mounted read-only in
`rust@sha256:3ddf079542eb8513a989f55bc68de3f3dff469bf8916400d955ce3a80a9ff8f0`
on Linux ARM64. `cargo test --locked --all-features` and
`cargo test --locked --no-default-features` each passed 163 tests with zero
failures and the single explicit live test ignored, for 326 ordinary passes.
Formatting, warnings-denied all-target/all-feature Clippy, and the locked
no-default release build passed. The local Linux ARM64 binary reported exactly
`docmost-local-mcp 0.9.5` and had SHA-256
`927051d39d01b7357868baf0cda1b4a8f9fc8b3348b4a5c618ae474b509ca115`.

The repository transcript's final optional `file` invocation found that the
slim Rust image does not contain that utility. All required Cargo commands had
already exited zero; the native provider transcript independently supplies the
Linux ARM64 ELF header and exact binary version. Release-integrity,
cosign-installer, version-consistency, release-context, immutable-input, and
`git diff --check` gates also passed. Provider dependency policy passed on the
exact source commit.

The MCP 2.0 focused run passed discovery fallback, initialize-first behavior,
product-owned `serverInfo` name/title/version, negotiation of `2025-03-26`,
exact ten-read inventory, synthetic registration readback/deletion, and clean
teardown.

## Disposable Docmost Community v0.95.0 matrix

The live boundary used the digest-pinned Docmost v0.95.0, Postgres 16, and
Redis 7.2 images in one uniquely labelled three-container, three-volume
Compose project on an unused loopback listener. The identity, workspace,
spaces, pages, comments, titles, bodies, and descriptions were synthetic.
Secrets were generated inside a mode-0700 card directory, used session-only,
never printed or retained, and removed after teardown.

The exact candidate directly established:

- protocol `2025-03-26`;
- default inventory of exactly ten reads and zero writes;
- all thirteen write names unavailable in the default process;
- exact thirteen-name write inventory and a separate five-name allowlist;
- all eight unallowlisted writes unavailable;
- successful execution of all ten reads;
- two page creates plus page update, page move, comment create, and comment
  update through the allowlisted MCP process;
- fresh direct readback of the updated title, parent relationship, and comment;
- origin-scoped forget with zero remaining matrix state files; and
- live independent effects for `delete_page`, `delete_comment`, and
  `delete_space`, including two trashed pages, two absent comments, and an
  absent space/page/comment chain.

Direct database inspection before infrastructure teardown found zero test
spaces matching the five synthetic slugs, zero pages, and zero comments. One
Docmost-created personal space remained with the synthetic user and was removed
with the disposable database volume.

## Authority, destructive, hostile-path, and sanitation gates

Focused candidate-bound tests passed the exact thirteen-write allowlist,
read-only defaults, invalid authority rejection, environment/CLI precedence,
per-tool schemas, conservative persistent-mutation annotations, and stable
delete target/consequence metadata.

The destructive matrix passed exact v0.95.0 routes and payloads, pre-dispatch
malformed-target rejection, absent/401/403/409/500 no-retry handling,
single-effect duplicate invocation, redirect refusal, ambiguous timeout without
retry, oversized success rejection after one dispatch, and interruption without
replay.

The hostile-path and sanitation matrix passed redirect refusal with no target
contact, production request deadlines, declared and streamed body caps,
one-byte-over input rejection, safe 403/500 errors, bounded authentication
responses, and the diagnostic allowlist. A retained-content scan found no
credential, token, cookie, authorization header, synthetic email, password,
application secret, database password, private response value, or private
origin.

## Cleanup and invariance

Teardown removed all three disposable containers, all three volumes, the
project network, loopback listener, synthetic database/content/identity,
credentials, sessions, homes, source-adjacent harnesses, target directories,
Cargo cache, downloaded provider artifacts, candidate binaries, and the
Docmost test image pulled by this card. Exact post-cleanup queries returned zero
project containers, volumes, and networks, and the card execution directory is
absent.

The non-card running-container count remained `4`; the complete sanitized
inventory hash was byte-identical before and after:
`8178ba6f68e526fb65032d7996b4967959b6764be336c8f55db4413a9821e210`.
Ordinary services were therefore neither recreated nor restarted.

All retained evidence is checksum-bound by
`verification/v0.9.5-candidate-SHA256SUMS` and the nested platform/log
manifests. This record authorizes no publication, release, installation,
activation, production registration, or ordinary Docmost access.
