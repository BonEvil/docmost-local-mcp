# v0.9.3 Atlas authorization and security-control evidence

Status: **passed** for the exact v0.9.3 source tree. This card did not access or
mutate any ordinary or production Docmost resource. All service behavior came
from disposable loopback origins, synthetic identities, and hostile synthetic
responses. The machine-checkable summary is
[`v0.9.3-atlas-control-evidence.json`](v0.9.3-atlas-control-evidence.json).

## Exact candidate and runtime

The supplied source candidate is
`a33895bef1c66c3bb0855c4d2ee06cef252c020f`, tree
`327d122cf9695b586e6999142a101b86bda4f67a`. The assigned integration commit
`b93024c6094abe5044b56f32991e39d650c6c9e5` has that identical tree. The
candidate was rebuilt with Rust 1.98.0, `--locked --release
--no-default-features`, in a native Linux ARM64 slim-bookworm image. The tested
binary SHA-256 was
`5f30cba8dca4bc5ce26100f5ca835f47808e45d4599eae6c73e1b7102bb0bc9b`;
the lockfile SHA-256 was
`3f2a639c0bed73088017f70fe9564a7d1696c24b4884e51bccf20284ce4754b9`.

Atlas ran from a read-only export of reviewed commit
`efad3719b67fc9949be3809a7d07b297a64de10d`, tree
`58d8b8c5d330c905ef70b5be33b06883c0a57ae6`. Each phase used a new isolated
Atlas database and removed its candidate registration before closing.

## Default authority and exact inventory

Fresh MCP negotiation selected protocol `2025-03-26` and returned exactly ten
tools, all annotated read-only:

```text
get_comments
get_current_user
get_page
get_space
list_child_pages
list_pages
list_spaces
list_workspace_members
search_docs
search_pages
```

Direct calls to all ten known mutation names, including the five legacy writes
and the later space/duplicate/copy/move-to-space operations, each returned
`code=-32602` / `tool not found`. Default write authority was therefore zero.

The exact tree also passed 44 focused tests with zero failures: eight auth-flow,
one integrated-policy, seven MCP-server, five network-safety, fifteen startup
configuration, and eight state-store tests. These cover the exact inventory,
allowlist validation, write annotations, identity rollover, 401 rollback
prevention, expiry parsing, origin pinning, scoped forget, cross-origin and
legacy-state refusal, redirect refusal, response bounds, timeouts, and safe
diagnostics.

## Atlas approve-once, denial, expiry, and replay

The live candidate registration reported `list_spaces` as read-only. Atlas's
empty safe-read policy still independently classified it as effect `write` and
required confirmation; the annotation granted no dispatch authority.

Two concurrent identical calls shared one pending authorization and both
remained blocked before a decision. Approve-once permitted exactly one dispatch
to an intentional hostile `403` loopback origin. Atlas recorded
`dispatch_failed` for that one attempted read; the competing call failed with
`mcp_confirmation_already_consumed`. Re-approving the consumed authorization
returned HTTP 409, with no replay dispatch.

A separately pending call was denied and returned
`mcp_confirmation_denied`; its dispatch outcome remained null. A third call
expired at the Atlas gate and returned `mcp_confirmation_expired`; its dispatch
outcome also remained null. Confirmation and lifecycle projections used only
the generic labels `Configured MCP server` and `Generic MCP tool` and contained
none of the synthetic argument, canary, origin, or host values.

The dispatch-effect ledger for the card is therefore:

```text
approved hostile read dispatches=1
denied dispatches=0
expired dispatches=0
replay dispatches=0
unauthorized dispatches=0
test writes=0
production accesses=0
production writes=0
```

## Identity, expiry, forget, and hostile paths

Six session-only synthetic identity flows created six origin-scoped sessions
and zero credential files. Every state directory was mode `0700` and every
state file was mode `0600`. A live forced-expiry run mutated the stored expiry,
required a new interactive loopback flow, accepted only the synthetic login,
and retained one renewed session with zero credential files.

The live forget matrix began with two origin-scoped sessions in one disposable
home. Forgetting the first origin exited zero and left exactly the other
origin's session. Repeating the first forget exited zero without changing the
remaining session. Forgetting the second origin exited zero and left zero
sessions and zero credentials.

The exact candidate then ran redirect, timeout, declared-oversize,
chunked-oversize, permission-denied, and server-error cases. All six failed
closed, every MCP process remained alive after its failure, the timeout fired
at 30.0 seconds, and the redirect target received zero requests. Searches of
the MCP and Atlas-visible failure projections found zero synthetic credential,
token, content, identity, origin, host, or redirect-target values.

## Cleanup and absence proof

Every isolated Atlas phase reported zero registered servers after deletion.
The hostile server controller reported its disposable root removed. The exact
card-labeled container was removed and a subsequent label-filtered container
query was empty. The downloaded harness image was untagged and deleted. The
candidate binary, build targets and registry caches, Atlas source export,
isolated Atlas databases, disposable password file, harness scripts, hostile
homes, and card run-state directories were removed. Exact post-cleanup path
checks reported all six generated directory targets absent.

No registration, binary, credential, session, temporary home, container,
database, or test resource created by this card remains. Only this sanitized
evidence is retained.
