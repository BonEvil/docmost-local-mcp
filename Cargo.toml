[package]
name = "docmost-local-mcp"
version = "0.9.4"
edition = "2024"
description = "Docmost MCP server for local IDE integrations"
license = "MIT"
repository = "https://github.com/BonEvil/docmost-local-mcp"
homepage = "https://github.com/BonEvil/docmost-local-mcp"

[profile.release]
codegen-units = 1
lto = "thin"
strip = "symbols"

[features]
# The native GTK/WebKit webview stack was removed from supported builds because it
# pulled an unmaintained Linux dependency chain.  The retained feature name is a
# no-op compatibility switch; authentication always uses the system browser.
default = []
native-webview = []

[dependencies]
aes-gcm = "0.10.3"
anyhow = "1.0.100"
axum = "0.8.4"
base64 = "0.22.1"
chrono = { version = "0.4.42", features = ["clock", "serde"] }
clap = { version = "4.5.48", features = ["derive", "env"] }
dirs = "6.0.0"
futures = "0.3.31"
keyring = "3.6.3"
once_cell = "1.21.3"
open = "5.3.2"
pulldown-cmark = { version = "0.13", default-features = false }
regex = "1.11.3"
reqwest = { version = "0.12.23", default-features = false, features = ["json", "multipart", "rustls-tls"] }
rmcp = { version = "0.6.1", features = ["transport-io", "client"] }
schemars = "1.0.4"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
sha2 = "0.10.9"
tokio = { version = "1.47.1", features = ["full"] }
tracing = "0.1.41"
tracing-subscriber = { version = "0.3.20", features = ["fmt"] }
url = "2.5.8"
urlencoding = "2.1.3"

[dev-dependencies]
tempfile = "3.23.0"
